name: CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  prepare:
    name: 'Prepare workflow'
    uses: eliashaeussler/gha/.github/workflows/preparation.yaml@25277ecb47b0e804f4cc464509789a0183cb78fc # 0.7.3

  checks:
    name: 'Checks'

    needs: [prepare]
    if: ${{ needs.prepare.outputs.continue == 'true' }}

    permissions:
      contents: read
      security-events: write

    uses: eliashaeussler/gha/.github/workflows/checks.yaml@25277ecb47b0e804f4cc464509789a0183cb78fc # 0.7.3
    with:
      composer: false

  python-checks:
    name: 'Perform Python checks'
    runs-on: ubuntu-latest

    needs: [prepare]
    if: ${{ needs.prepare.outputs.continue == 'true' }}

    permissions:
      contents: read

    steps:
      - name: 'Checkout'
        uses: eliashaeussler/gha/.github/actions/checkout@25277ecb47b0e804f4cc464509789a0183cb78fc # 0.7.3

      - name: 'Setup Python'
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: 3
          cache: 'pip'

      - name: 'Install dependencies'
        run: pip install -r requirements.txt -r requirements-dev.txt

      - name: 'Lint Editorconfig'
        run: ec -verbose

      - name: 'Lint Python files'
        run: pylint lib/*.py

      - name: 'Lint shell files'
        run: bash -c 'shopt -s globstar nullglob; shellcheck *.sh'
